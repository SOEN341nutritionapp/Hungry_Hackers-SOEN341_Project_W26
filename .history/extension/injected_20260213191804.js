// Runs in the page context (not extension context).
// We'll use this later if Metro blocks DOM access or data is in JS variables.
console.log("[Metro→Fridge] injected script running in page context");
